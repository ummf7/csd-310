User fields
.user_id
    .INT
    .NOT NULL
    .AUTO_INCREMENT
    .PRIMARY KEY
.first_name
    .VARCHAR 75
    .NOT NULL
.Last_name
    .VARCHAR 75
    .NOT NULL
Wishlist fields
.wishlist_id
    .INT
    .NOT NULL
    .AUTO_INCREMENT
    .PRIMARY KEY
.user_id
    .INT
    .NOT NULL
    .AUTO_INCREMENT

.book_id
    .INT
    .NOT NULL
    .AUTO_INCREMENT
FOREIGN KEY
Book fields
.book_id
    .INT
    .NOT NULL
    .AUTO_INCREMENT
PRIMARY KEY
.book_name
        .VARCHAR 200
        .NOT NULL
details V
        .VARCHAR(500)
.author
        .VARCHAR(200)
        .NOT NULL
Store fields
.store_id
    .INT
    .NOT NULL
    .PRIMARY KEY
.locale
    .VARCHAR(500)
        .this will be a string field that will include the street address, city, state, and zip
        .NOT NULL
